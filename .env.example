# Free key from https://api.data.gov/signup/ . Without it the FBI Crime Data Explorer
# falls back to the shared DEMO_KEY, which allows only a few requests per hour.
FBI_CRIME_API_KEY=

# Optional. Official U.S. Census key for the civic map. The build falls back to
# Census Reporter when this is absent.
CENSUS_API_KEY=
